# GitHub Personal Access Token (read-only, public repos scope)
# Use a read-only PAT scoped to public repos only. This token is visible in the browser bundle.
VITE_GITHUB_TOKEN=
# GitHub username whose repositories to display
VITE_GITHUB_USERNAME=Azeemme

